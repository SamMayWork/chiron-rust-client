apiVersion: v1
kind: Service
metadata:
  name: {{ include "chiron-client.fullname" . }}
  labels:
    {{- include "chiron-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chiron-client.selectorLabels" . | nindent 4 }}
